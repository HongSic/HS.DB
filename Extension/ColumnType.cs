namespace HS.DB.Extension
{
    public enum ColumnType
    {
        ETC = -1,
        AUTO = 0, //DB 형식에 따름
        NUMBER,
        DECIMAL,
        STRING,
        DATETIME,
        BIN,
        BOOL,
    }
}
